import React from 'react';
import FormLinks from './FormLinks';

import '../styles/main.scss';

const FormStats = () => {
  return (
    <section className={'form__stats flex flex__jc-c'}>
      <FormLinks />
    </section>
  );
};

export default FormStats;
